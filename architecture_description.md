# Proposed Architecture – Member 3

**Flow:** Activity/Log Sources → Data Collection → Preprocessing → Feature Extraction → ML/Anomaly Detection → Risk Decision → Alert & Logging → Evaluation

**Input examples:** user activity logs, network metadata, endpoint events, email/cloud/other DLP event records.

**Important:** At proposal stage this is a preliminary architecture, not a complete production DLP deployment.
